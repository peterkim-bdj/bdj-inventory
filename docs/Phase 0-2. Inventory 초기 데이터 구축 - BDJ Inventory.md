---
created: 2026-02-05
tags:
  - BDJ-Inventory
  - Phase0
  - Inventory
  - Barcode
  - Warehouse
source_url: 
source_type: Other
channel: "[[BDJ Inventory]]"
status: Summarized
---

# Phase 0-2: Inventory 초기 데이터 구축 - BDJ Inventory

> [!tldr] 💡 핵심 한 줄
> 창고 실물 재고를 시스템에 등록하는 **기본 기능(바코드 스캔)**과, 초기 대량 셋업을 가속하는 **보조 방법(사진 기반 일괄 등록)**.
>
> Core system feature: barcode scan registration (used for initial setup + ongoing operations). Supplementary method: photo-based bulk registration via Claude Code (accelerates initial setup, operated externally).

## 핵심 요약

Phase 0-1에서 Shopify 상품 데이터를 DB에 구축한 후, Phase 0-2에서는 **실제 창고에 있는 재고**를 시스템에 등록한다.

### 바코드 스캔 (필수) + 사진 기반 일괄 (보조)

**바코드 스캔 등록**은 시스템의 **핵심 내장 기능**이다. 초기 재고 등록은 물론, 반품 입고·신규 입고·재고 실사 등 운영 전반에서 계속 사용된다. 따라서 반드시 구현해야 한다.

**사진 기반 일괄 등록**은 초기 셋업 속도를 높이기 위한 **보조 방법**이다. 다만 시스템 내장 기능이 아니라 **외부 도구(Claude Code)**로 처리되므로, 실제 사용 업체에서 직접 실행하려면 별도 Claude 결제가 필요하거나, 개발자가 사진을 전달받아 대신 처리하는 형태가 된다.

| | 바코드 스캔 등록 (필수) | 사진 기반 일괄 등록 (보조) |
|---|---|---|
| **성격** | 시스템 내장 기능 | 외부 보조 도구 (Claude Code) |
| **용도** | 초기 등록 + 운영 전반 | 초기 대량 등록 가속 |
| **속도** | 건별 (정밀) | 선반 단위 (빠름) |
| **정확도** | 높음 (1:1 매칭) | 반자동 (사람 리뷰 필요) |
| **사용 주체** | 실제 사용 업체 | 개발자 또는 업체(Claude 결제 시) |
| **운영 단계** | ✅ 계속 사용 | ❌ 초기에만 |

> [!warning] 사진 기반 등록의 제약
> - 실제 사용 업체에 Claude Code가 없음
> - **방법 1**: 업체가 사진 촬영 → 개발자에게 전달 → 개발자가 Claude Code로 처리
> - **방법 2**: 업체가 Claude를 결제하여 직접 Claude Code 사용
> - 어느 쪽이든 시스템 자체 기능이 아니므로, 바코드 스캔이 기본이어야 함

## 주요 내용

---

## 바코드 스캔 등록 (시스템 핵심 기능)

> [!info] 사용 시점
> - **초기 재고 등록** (Phase 0-2 셋업)
> - 반품 입고 시 재고 재등록
> - 벤더에서 신규 상품 입고 시
> - 재고 실사 시 개별 확인
> - 일상 운영 전반

### 재고 등록 플로우
```
[1. 바코드 스캔]
   - 바코드 스캐너 (USB/Bluetooth)
   - 모바일 카메라 (PWA/웹앱)
   - 수동 텍스트 입력
        │
        ▼
[2. Product 자동 검색]
        │
   ┌────┴────┐
   ▼         ▼
[매칭됨]   [매칭 안됨]
   │         │
   │    [4. 신규 Product 생성]
   │    ⚠️ Shopify 미싱크 표시
   │         │
   └────┬────┘
        ▼
[3. 수량 입력 + 위치 선택]
   - 수량 직접 입력 또는 +1 버튼
   - 위치: 1층 / 지하 (현재)
   - 향후: 선반·구역 단위로 확장
        │
        ▼
[5. InventoryItem 생성 + 바코드 출력]
   - 수량만큼 개별 아이템 생성
   - 각 아이템에 고유 바코드
   - 라벨 출력 → 실물에 부착
        │
        ▼
[6. 1번으로 돌아가기 (반복)]
```

### 스캔 입력 방식

| 방식 | 디바이스 | 기술 | 장점 |
|------|---------|------|------|
| 바코드 스캐너 | PC/태블릿 | USB/Bluetooth HID | 가장 빠름, 정확함 |
| **모바일 카메라** | 스마트폰 | Web API (BarcodeDetector) 또는 라이브러리 | 별도 장비 불필요 |
| 수동 입력 | 모든 기기 | 텍스트 입력 | 바코드 없을 때 |

> [!info] 모바일 카메라 스캔
> - **BarcodeDetector API** (Chrome 83+): 네이티브 브라우저 지원
> - **폴백**: `html5-qrcode` 또는 `quagga2` 라이브러리
> - PWA로 만들면 홈 화면에 추가하여 앱처럼 사용 가능
> - 카메라 권한만 필요, 별도 앱 설치 불필요

### 모바일 간소화 UI
```
┌──────────────────────────┐
│  📦 재고 등록     [📷]   │
├──────────────────────────┤
│                          │
│  ┌──────────────────┐   │
│  │  📸 카메라 뷰    │   │
│  │                  │   │
│  │  [바코드 인식중] │   │
│  └──────────────────┘   │
│                          │
│  ✅ Air Max 90 - Black   │
│     Nike | BDJ Main      │
│                          │
│  수량: [-] [ 5 ] [+]    │
│                          │
│  위치: [1층 ▼]           │
│                          │
│  [🏷️ 저장 & 바코드 출력] │
│                          │
│  ── 최근 ──              │
│  Air Max 90 ×5 지하 방금  │
│  Galaxy S24 ×3 1층 2분전  │
└──────────────────────────┘
```

> [!tip] 모바일 UX 핵심
> - 카메라 뷰가 상단에 항상 활성화
> - 스캔 성공 시 **진동 피드백** + 상품 정보 자동 표시
> - 수량·위치만 터치로 입력
> - 저장 후 자동으로 카메라 뷰로 복귀 → **연속 스캔**

### 각 스텝 상세

#### Step 1: 바코드 스캔

- 바코드 스캐너: USB/Bluetooth → input 필드에 자동 입력
- 모바일 카메라: BarcodeDetector API 또는 html5-qrcode 라이브러리
- 수동 입력: 텍스트 필드에 직접 타이핑

#### Step 2: 자동 검색 (우선순위)

| 순서 | 검색 대상 | 매칭 방식 |
|------|----------|----------|
| 1차 | shopifyBarcode | 정확 매칭 |
| 2차 | sku | 정확 매칭 |
| 3차 | name | 부분 매칭 (contains) |

- 매칭 결과가 여러 개면 목록에서 선택
- 매칭 결과에 **소속 몰(ShopifyStore)** 표시

#### Step 3: 수량 + 위치 입력 (매칭된 경우)

- 수량: 직접 숫자 입력 또는 **+1 버튼** (연속 스캔)
- 위치 선택: 현재는 1층, 지하 2가지

> [!info] 위치 확장성
> 현재는 단순 2개 (1층/지하)이지만, 나중에 **선반 > 구역 > 칸** 단위로 세분화될 수 있음.
> Location 테이블을 **계층 구조(self-referencing)**로 설계하여 확장성 확보.

#### Step 4: 신규 Product 생성 (매칭 안 된 경우)

- 최소 정보로 Product 생성: 이름, SKU/바코드 (스캔한 값)
- **`shopifySynced: false`** 표시
- 나중에 Shopify와 수동 매핑 또는 자동 싱크 필요

> [!warning] Shopify 미싱크 상품
> `shopifySynced` 플래그로 관리. Product View에서 "미싱크" 필터로 확인 가능.
> 나중에 Shopify에 상품이 등록되면 매핑하는 기능 필요 (Phase 2).

#### Step 5: 바코드 출력 + 부착

- 수량만큼 [[InventoryItem]] 레코드 생성
- 각 아이템에 고유 바코드: `{barcodePrefix}-001`, `-002`, `-003`...
- 라벨 프린터로 출력
- 실물 아이템에 바코드 스티커 부착

#### Step 6: 반복

- 저장 후 자동으로 스캔 모드로 복귀
- 모바일: 카메라 뷰 유지 → 연속 스캔

### 필요한 테이블 스키마

> [!note] 스키마 최종 기준: [[Schema Reference - BDJ Inventory]]
> 아래 스키마는 이 Phase에서 도입된 모델의 요약이다. 필드 추가/변경은 Schema Reference 문서를 기준으로 한다.

#### Location (신규)
```prisma
model Location {
  id          String     @id @default(cuid())
  name        String                           // "1층", "지하"
  code        String     @unique               // "F1", "B1"
  parentId    String?
  parent      Location?  @relation("LocationTree", fields: [parentId], references: [id])
  children    Location[] @relation("LocationTree")
  level       Int        @default(0)           // 0=건물, 1=층, 2=구역, 3=선반
  description String?
  isActive    Boolean    @default(true)
  createdAt   DateTime   @default(now())
  updatedAt   DateTime   @updatedAt

  items       InventoryItem[]
}
```

**계층 구조 예시:**
```
현재:
├── 1층 (F1)
└── 지하 (B1)

나중에:
├── 1층 (F1)
│   ├── A구역 (F1-A)
│   │   ├── 선반1 (F1-A-1)
│   │   └── 선반2 (F1-A-2)
│   └── B구역 (F1-B)
└── 지하 (B1)
    └── A구역 (B1-A)
```

#### InventoryItem (신규)
```prisma
model InventoryItem {
  id          String          @id @default(cuid())
  barcode     String          @unique            // "BDJ-A1B2C3-001"
  productId   String
  product     Product         @relation(fields: [productId], references: [id])
  locationId  String?
  location    Location?       @relation(fields: [locationId], references: [id])
  status      InventoryStatus @default(AVAILABLE)
  condition   ItemCondition   @default(NEW)
  notes       String?
  receivedAt  DateTime        @default(now())
  soldAt      DateTime?
  createdAt   DateTime        @default(now())
  updatedAt   DateTime        @updatedAt

  @@index([productId])
  @@index([locationId])
  @@index([status])
  @@index([barcode])
}

enum InventoryStatus {
  AVAILABLE
  RESERVED
  SOLD
  RETURNED
  DAMAGED
}

enum ItemCondition {
  NEW
  LIKE_NEW
  GOOD
  FAIR
  POOR
}
```

#### Product (전체 스키마 - shopifySynced 추가)
```prisma
model Product {
  id                String    @id @default(cuid())

  // 기본 정보
  name              String
  description       String?
  imageUrl          String?                      // Shopify CDN URL (public)

  // 검색용
  sku               String?
  shopifyBarcode    String?                      // 제조사 바코드 (UPC/EAN)
  barcodePrefix     String    @unique            // 개별 아이템 바코드 생성용

  // 분류
  productType       String?

  // 가격
  price             Decimal?
  compareAtPrice    Decimal?                     // 정가 (할인 전)

  // Vendor 연결
  vendorId          String?
  vendor            Vendor?   @relation(fields: [vendorId], references: [id])
  vendorName        String?                      // Shopify 원본 (매핑 전 백업)

  // Shopify 연결
  shopifyProductId  String?
  shopifyVariantId  String?
  shopifyStoreId    String?
  shopifyStore      ShopifyStore? @relation(fields: [shopifyStoreId], references: [id])
  shopifySynced     Boolean   @default(true)     // false = 수동 생성, Shopify 미싱크

  // 시스템
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  // 관계
  items             InventoryItem[]

  // 인덱스
  @@index([sku])
  @@index([shopifyBarcode])
  @@index([name])
  @@index([vendorId])
  @@index([productType])
  @@index([shopifyStoreId])
  @@index([shopifySynced])
  @@unique([shopifyStoreId, shopifyProductId, shopifyVariantId])
}
```

### 바코드 체계
```
제조사 바코드:     8801234567890         (Shopify barcode)
      ↓
BDJ barcodePrefix: BDJ-A1B2C3           (Product 생성 시 자동 생성)
      ↓
개별 아이템 바코드: BDJ-A1B2C3-001      (InventoryItem)
                    BDJ-A1B2C3-002
                    BDJ-A1B2C3-003
```

### 데스크톱 UI 와이어프레임
```
┌─────────────────────────────────────────────────────┐
│  📦 재고 등록                                       │
├─────────────────────────────────────────────────────┤
│                                                     │
│  🔍 [스캔 또는 입력...                      ] [검색] │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ ✅ 매칭됨                                   │   │
│  │                                              │   │
│  │ 🖼  Air Max 90 - Black                      │   │
│  │     SKU: AM90-BLK | Nike | BDJ Main         │   │
│  │                                              │   │
│  │ 수량: [ 5  ] [+1]                           │   │
│  │ 위치: ( ) 1층   (●) 지하                    │   │
│  │                                              │   │
│  │ [🏷️ 바코드 출력 & 등록]                     │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ── 또는 ──                                         │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │ ⚠️ 매칭 안됨 → 스캔값: "1234567890"        │   │
│  │                                              │   │
│  │ 상품명: [                               ]   │   │
│  │ [➕ 신규 상품 생성] ⚠️ Shopify 미싱크       │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ── 최근 등록 내역 ──                                │
│  │ BDJ-A1B2C3-001~005 │ Air Max 90 │ 지하 │ 방금 │ │
│  │ BDJ-X9Y8Z7-001~003 │ Galaxy S24 │ 1층  │ 2분전│ │
└─────────────────────────────────────────────────────┘
```

### 초기 Location 시드 데이터
```typescript
const locations = [
  { name: '1층', code: 'F1', level: 1 },
  { name: '지하', code: 'B1', level: 1 },
];
```

---

## 보조: 사진 기반 일괄 등록 (초기 셋업 가속용)

> [!warning] 시스템 외부 도구
> 이 방법은 **시스템 내장 기능이 아니다**. Claude Code를 사용하여 외부에서 처리한다.
> - **방법 1**: 업체가 사진 촬영 → 개발자에게 전달 → 개발자가 Claude Code로 처리
> - **방법 2**: 업체가 Claude를 결제하여 직접 Claude Code 사용
> - 바코드 스캔 등록이 기본이며, 이 방법은 초기 대량 등록 시 **보조적으로 병행**한다.

### 핵심 아이디어

선반/구역별로 **상품 태그가 보이게 사진을 촬영**하고, 파일명에 위치 정보를 인코딩한다. Claude Code agent/skill이 사진에서 SKU/바코드를 자동 추출하여 Product DB와 매칭하고, 리뷰 시트를 생성한다. 사람이 검토·수정 후 최종 Import한다.

### 사진 촬영 방법

**최선**: 태그만 따로 떼어내서 모아서 촬영 (가장 높은 인식률)
**차선**: 제품을 그룹으로 모아서 태그가 보이게 촬영
**다수 상품**: 한 사진에 다 안 찍히면 여러 장으로 분할 촬영

### 파일명 컨벤션
```
{위치}-{선반}-{층수}-{박스}-{사진번호}.jpg
```

| 세그먼트 | 설명 | 예시 |
|---------|------|------|
| 위치 | 1F (1층) / B1 (지하) | `1F` |
| 선반 | 선반 이름 | `A` |
| 층수 | 선반 내 층 번호 | `2` |
| 박스 | 박스 또는 구역 번호 | `1` |
| 사진번호 | 같은 위치의 N번째 사진 | `1` |

**예시:**
```
1F-A-2-1-1.jpg   → 1층, A선반, 2층, 1번 박스, 사진 1
1F-A-2-1-2.jpg   → 1층, A선반, 2층, 1번 박스, 사진 2 (추가 사진)
B1-C-1-3-1.jpg   → 지하, C선반, 1층, 3번 박스, 사진 1
```

> [!info] 파일명 = 자동 위치 등록
> 파일명을 파싱하면 Location 정보가 자동으로 추출된다.
> `1F-A-2-1` → Location: 1층 > A선반 > 2층 > 1번 박스

### 처리 플로우

```
[1. 사진 촬영] (업체)
   - 선반/구역별로 태그가 보이게 촬영
   - 파일명 규칙에 맞게 저장
   - 개발자에게 전달
        │
        ▼
[2. Claude Code agent 분석] (개발자 또는 업체)
   - 파일명 파싱 → 위치(Location) 추출
   - 이미지 비전 분석 → SKU / 바코드 텍스트 추출
   - Phase 0-1 Product DB 매칭
        │
   ┌────┴────┐
   ▼         ▼
[매칭됨]   [매칭 불확실/안됨]
   │         │
   │    확신도 표시 (LOW/MEDIUM/HIGH)
   │         │
   └────┬────┘
        ▼
[3. 리뷰 시트 생성]
   - 위치, 상품명, SKU, 매칭확신도
   - 수량은 사람이 입력/확인 필요
   - 매칭 실패 항목 하이라이트
        │
        ▼
[4. 사람 리뷰] (업체 + 개발자)
   - 매칭 결과 확인/수정
   - 수량 입력 (사진만으로 정확한 수량 파악 어려움)
   - 매칭 실패 → 수동 매핑 또는 신규 Product 생성
        │
        ▼
[5. 최종 Import]
   - 확인된 데이터로 InventoryItem 일괄 생성
   - 개별 바코드 발급 (필요 시)
   - Location 자동 생성 (파일명 기반)
```

### 리뷰 시트 예시

| 사진 | 위치 | 감지된 SKU | 매칭 상품 | 확신도 | 수량 (입력) | 상태 |
|------|------|-----------|----------|--------|------------|------|
| 1F-A-2-1-1.jpg | 1F > A > 2 > 1 | SKU-001 | 상품 A | HIGH | ___ | ✅ 확인 대기 |
| 1F-A-2-1-1.jpg | 1F > A > 2 > 1 | SKU-002 | 상품 B | MEDIUM | ___ | ⚠️ 확인 필요 |
| 1F-A-2-1-2.jpg | 1F > A > 2 > 1 | (읽기 실패) | — | — | ___ | ❌ 수동 매핑 |

### Claude Code Agent 상세

```
역할: 사진에서 상품 식별 정보를 추출하여 Product DB와 매칭

입력:
  - 촬영된 사진 파일들 (파일명 규칙 준수)
  - Product DB (Phase 0-1에서 구축)

처리:
  1. 파일명 파싱 → Location 계층 구조 자동 생성
  2. 이미지 비전 → 태그에서 텍스트 추출 (SKU, 바코드 숫자, 상품명)
  3. 추출된 텍스트 → Product DB 매칭
     - shopifyBarcode 정확 매칭 (HIGH)
     - SKU 정확 매칭 (HIGH)
     - 상품명 부분 매칭 (MEDIUM)
     - 매칭 실패 (LOW / NONE)
  4. 결과를 리뷰 시트(CSV/엑셀)로 출력

출력:
  - 리뷰 시트 (위치, SKU, 매칭 상품, 확신도, 수량 빈칸)
  - 매칭 실패 목록 (수동 처리 필요)
  - 위치별 요약 (Location당 몇 종류, 대략 몇 개)
```

> [!warning] 수량 파악의 한계
> 사진만으로는 정확한 수량을 파악하기 어렵다. 같은 상품이 3개인지 5개인지 사진에서 확인하기 어려울 수 있다.
> → **수량은 반드시 사람이 입력/확인**해야 한다. Agent는 "이 위치에서 이 상품을 발견했다"까지만 자동화.

> [!tip] 사진 촬영 팁
> - 태그의 SKU/바코드가 **선명하게** 보이도록 촬영
> - 가능하면 태그만 모아서 촬영 (인식률 최고)
> - 한 사진에 너무 많은 상품을 넣지 않기 (10개 이내 권장)
> - 조명이 충분한 환경에서 촬영
> - 촬영 전 파일명 규칙 숙지

---

## 핵심 인사이트

- **바코드 스캔이 기본**: 시스템 내장 기능. 초기 등록 + 반품/신규 입고 + 실사 등 운영 전반에서 사용
- **사진 기반은 보조**: 초기 대량 등록 가속용. Claude Code 외부 도구이므로 업체 자체 기능이 아님
- **모바일 카메라 스캔**: BarcodeDetector API 또는 html5-qrcode로 별도 앱 없이 웹에서 바코드 인식 가능
- **모바일 간소화 UI**: 카메라 + 수량 + 위치만 표시하여 창고에서 빠른 연속 등록
- **Location 계층 구조**: self-referencing relation으로 현재 2개 → 나중에 선반·구역 무한 확장
- **shopifySynced 플래그**: 수동 생성 Product 구분 → 나중에 Shopify 매핑 워크플로우 지원
- **개별 바코드**: 각 아이템 고유 추적 가능 (반품, 위치 이동 등)

## 체크리스트

### 바코드 스캔 등록 (필수)
- [ ] Location 스키마 생성 + 시드 데이터
- [ ] InventoryItem 스키마 생성
- [ ] Product에 shopifySynced 필드 추가
- [ ] InventoryItem 생성 API (수량 → 개별 아이템)
- [ ] 바코드 생성 로직 ({barcodePrefix}-{sequence})
- [ ] 바코드 라벨 출력 기능
- [ ] 신규 Product 생성 API (미싱크)
- [ ] 바코드 스캔 → Product 검색 API
- [ ] 모바일 카메라 바코드 인식 구현
- [ ] 데스크톱 재고 등록 UI
- [ ] 모바일 간소화 재고 등록 UI
- [ ] 최근 등록 내역 표시

### 사진 기반 일괄 등록 (보조 — 초기 셋업용)
- [ ] 파일명 파싱 로직 (위치 자동 추출)
- [ ] Claude Code agent/skill 구현 (이미지 비전 → SKU 추출 → DB 매칭)
- [ ] 리뷰 시트 생성 로직 (CSV/엑셀 출력)
- [ ] 리뷰 시트 → 최종 Import 처리
- [ ] 파일명 기반 Location 자동 생성
- [ ] 사진 촬영 가이드 문서 작성

## 관련 노트

- [[BDJ Phase0-1 Initial Data Setup]]
- [[BDJ Inventory - Phase 1 Development Strategy]]
- [[Shopify API Research]]
- [[Barcode System Design]]
- [[BarcodeDetector Web API]]

---

## 🌐 English Summary

**Phase 0-2: Inventory Registration** handles physical warehouse stock registration. The **core system feature is barcode scan registration**: scan barcodes via USB/Bluetooth scanner, mobile camera (BarcodeDetector API), or manual input → Product match → quantity/location input → individual barcode print. This is the built-in function used for initial setup AND ongoing operations (returns, new stock, audits). Additionally, a **supplementary photo-based bulk registration** method accelerates initial setup: photograph product tags by shelf/area, encode location in filenames (`{floor}-{shelf}-{level}-{box}-{photo#}.jpg`), then Claude Code agent (external tool, not system-built) analyzes images → generates review sheet → human confirms → bulk import. Key constraint: photo method requires Claude Code, which the end-user company may not have — either the developer processes photos on their behalf, or the company subscribes to Claude separately.